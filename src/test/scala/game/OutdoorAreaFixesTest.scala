package game

import map.{Dungeon, MapGenerator, TileType}
import org.scalatest.funsuite.AnyFunSuite

class OutdoorAreaFixesTest extends AnyFunSuite {
  
  // Tests commented out - outdoor rooms feature has been removed
  ignore("No enemies spawn in outdoor rooms") {
    // Feature removed - outdoor rooms no longer exist
  }
  
  ignore("Outdoor rooms do not have dungeon depth") {
    // Feature removed - outdoor rooms no longer exist
  }
  
  ignore("Solid wall exists between outdoor and dungeon areas with only doorway access") {
    // Feature removed - outdoor rooms no longer exist
  }
}

